<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" fill="none">
  <!-- Círculo exterior -->
  <circle cx="60" cy="60" r="56" stroke="currentColor" stroke-width="1.5"/>
  <!-- Línea interior fina -->
  <circle cx="60" cy="60" r="49" stroke="currentColor" stroke-width="0.5" opacity="0.35"/>
  <!-- Letras LD -->
  <text
    x="60" y="76"
    font-family="Fraunces, Georgia, 'Times New Roman', serif"
    font-size="38"
    font-weight="200"
    letter-spacing="7"
    text-anchor="middle"
    fill="currentColor"
    dx="-3">LD</text>
</svg>
